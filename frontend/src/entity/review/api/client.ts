// import { clientAxios } from "@share/api"
// import type { z } from "zod"
// import type { AddReview, reviewSchema } from "../consts/schema"

// export const clientAddReview = async (review: AddReview) => {
// 	const response = await clientAxios.post("/externalApi/review", review)
// }
