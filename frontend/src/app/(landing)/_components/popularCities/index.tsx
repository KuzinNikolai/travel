import { Box } from "@/components/Box"
import { Typography } from "@/components/Typography"

export const PopularCities = () => {
  return (
    <Box className="w-full px-2">
      <Typography variant="h2">Most Popular Cities</Typography>
    </Box>
  )
}